# DB-04B-R1 Review and Correction Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Corriger la baseline DB-04B avant tout apply, renforcer ses tests multi-écoles et produire un bundle de revue statique reproductible.

**Architecture:** La baseline reste un paquet PostgreSQL VPS from-zero, sans dépendance Supabase et sans accès direct navigateur. Le runner refusera toute application tant que `pg_stat_statements` n'est pas préchargé; un document séparé décrira uniquement la recréation manuelle du conteneur TEST avec le volume existant conservé.

**Tech Stack:** PostgreSQL 17.11 SQL, Bash Docker runbook non exécuté, Node.js `node:test`, SHA-256.

**Spec:** demande utilisateur DB-04B-R1 du 2 septembre 2026 et [plan DB-04B](../../../database/baseline/v1/DB-04B-EXECUTION-PLAN.md).

## Global Constraints

- Aucun SQL, Docker, PostgreSQL, Supabase Cloud ou PROD ne doit être modifié.
- Aucun commit ni push.
- Exactly 60 permissions et 7 scopes.
- Access_Law fail-closed; DENY explicite prioritaire.
- Enseignant = affectation active exacte classe ET matière, bornée à l'école.
- Aucun secret ni mot de passe dans Git.

---

### Task 1: Contract tests RED

**Files:**
- Modify: `database/baseline/v1/tests/static-contract.test.mjs`
- Create: `database/baseline/v1/tests/review-bundle.test.mjs`

**Interfaces:**
- Consumes: baseline DB-04B existante.
- Produces: échecs ciblés pour préchargement extension, trois écoles, isolation et bundle de revue.

- [ ] Écrire les assertions manquantes pour `pg_stat_statements`, School A/B/C, isolation, DENY, RPC cross-school et secrets.
- [ ] Exécuter `node --test database/baseline/v1/tests/*.test.mjs` et constater les échecs attendus.

### Task 2: Extension gate and container correction document

**Files:**
- Modify: `database/baseline/v1/03_extensions.sql`
- Modify: `database/baseline/v1/13_verification.sql`
- Modify: `database/baseline/v1/scripts/run-from-zero-test.sh`
- Create: `database/baseline/v1/review/CONTAINER_TEST_RECREATION.md`

**Interfaces:**
- Consumes: DB-03 parameters and DB-04A image/network/volume/secret contract.
- Produces: preflight blocking before unit 01 and future extension creation only after preload.

- [ ] Exiger `shared_preload_libraries=pg_stat_statements` avant toute unité.
- [ ] Installer/vérifier l'extension dans l'unité 03 pour le futur apply approuvé.
- [ ] Documenter les exactes commandes root de recréation sans les exécuter.
- [ ] Conserver `schoolsafe-pg-test-data` et le secret `/etc/schoolsafe-db/test/secrets/postgres_bootstrap_password`.

### Task 3: Multi-school semantic tests

**Files:**
- Modify: `database/baseline/v1/tests/from-zero-access-law.test.sql`

**Interfaces:**
- Consumes: `api.set_request_context`, `api.check_access`, forced RLS and P0 RPCs.
- Produces: fixtures School A/B/C intégralement annulées par `ROLLBACK`.

- [ ] Ajouter trois écoles, trois identités, leurs rôles, élèves, classes, matières et affectations actives exactes.
- [ ] Tester l'isolation RLS A/B/C dans les six directions.
- [ ] Tester `own_children`, affectation classe-matière, DENY school-local, contexte incohérent et RPC cross-school.
- [ ] Ne créer aucune table trial et n'utiliser que des adresses `.invalid`.

### Task 4: Reproducible review bundle

**Files:**
- Create: `database/baseline/v1/scripts/generate-review-bundle.mjs`
- Create: `database/baseline/v1/review/DB-04B-R1-REVIEW.md`
- Create: `database/baseline/v1/review/DB-04B-R1-REVIEW.json`

**Interfaces:**
- Consumes: neuf fichiers critiques demandés.
- Produces: SHA-256, lignes, objets, definer, ACL, RLS, dépendances et risques par fichier.

- [ ] Analyser les fichiers avec Node.js sans exécuter SQL ou shell.
- [ ] Générer Markdown et JSON déterministes.
- [ ] Vérifier les hashes du bundle contre les fichiers réels.

### Task 5: Secrets and final static review

**Files:**
- Modify: `database/baseline/v1/README.md`
- Modify: `database/baseline/v1/DB-04B-EXECUTION-PLAN.md`
- Regenerate: `database/baseline/v1/manifest.json`
- Regenerate: `database/baseline/v1/manifest.sha256`

**Interfaces:**
- Consumes: corrections Tasks 2-4.
- Produces: procédure hors Git pour les trois secrets runtime et rapport final cohérent.

- [ ] Documenter l'injection future via fichiers root `0600` montés en lecture seule et commandes `ALTER ROLE ... PASSWORD` via variables `psql` sans journalisation.
- [ ] Régénérer les checksums après toutes les corrections SQL.
- [ ] Exécuter tous les tests Node statiques et la validation déterministe du manifeste/bundle.
- [ ] Vérifier branche/HEAD/status; confirmer aucune application, aucun commit et aucun push.
